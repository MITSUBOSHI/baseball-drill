import Link from "next/link";
import { learningLessons } from "@/data/learning";
const chapters = ["打者評価","投手評価","戦術評価","総合評価"];
export default function LearnPage(){return <div className="inner-page learn-page"><section className="page-hero"><span className="kicker">COURSE 01 · SABERMETRICS</span><h1>セイバーメトリクス<br/><em>12のレッスン。</em></h1><p>打者・投手・戦術・総合評価。<br/>好きなテーマから、好きな順番で学べます。</p><div className="open-course-note">全レッスン公開中 · 回答によるロックはありません</div></section><section className="content-shell lessons-section"><div className="lesson-intro"><span>ALL LESSONS</span><div><h2>気になる数字から始めよう</h2><p>順番に進めても、観戦中に気になった指標だけ選んでもOKです。</p></div></div>{chapters.map((chapter,chapterIndex)=><section className="chapter-group" key={chapter}><header><span>CHAPTER {chapterIndex+1}</span><h2>{chapter}</h2></header><div className="lesson-list">{learningLessons.filter((lesson)=>lesson.chapter===chapter).map((lesson)=><Link href={`/learn/${lesson.slug}`} className="lesson-row" key={lesson.slug}><span className="lesson-num">{lesson.number}</span><div><h3>{lesson.title}</h3><p>{lesson.subtitle}</p></div><small>{lesson.minutes} min</small><span className="lesson-open">→</span></Link>)}</div></section>)}</section></div>}
